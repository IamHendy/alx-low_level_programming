#include "main.h"
#include <stdio.h>

/**
 * _strcmp - compares two strings
 * @s1: first parameter
 * @s2: second parameter
 *Return: string
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
