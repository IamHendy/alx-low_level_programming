#include "main.h"

/**
 * string_toupper - changes all lowercase letters of a screen to uppercase
 * @str: parameter
 * Return: returns a character
 */

char *string_toupper(char *str)
{
	int len = strlen (s);

	int i;



	for (i = 0; i < len; i++)

	{

		s[i] = toupper(s[i]);



	}

	return (s);

}
